import {IBase} from '../utility/helpers/ibase';

export class IPeople extends IBase {
  Name: string;
  Height: number;
  Mass: number;
  HairColor: string;
  SkinColor: string;
  EyeColor: string;
  DateOfBirth: string;
  Gender: string;
  HomeWorld: string;
}
